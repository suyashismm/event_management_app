import { useState } from "react";

const MyEvents = ({createEvents}) => {

    const [myEvents,setMyEvents] = useState([]);

    return(
        <>
        myEvents.map((event) = {
            <div></div>
        })
        </>
    );
}